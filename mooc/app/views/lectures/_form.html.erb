<%= form_for(@lecture) do |f| %>
  <% if @lecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lecture.errors.count, "error") %> prohibited this lecture from being saved:</h2>

      <ul>
      <% @lecture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




<div class="panel panel-primary">
  <!-- Default panel contents -->
  <div class="panel-heading">Lecture Content </div>
  <div class="panel-body">
    
      <div class="field">
        <%= f.label :content %><br>
        <%= f.text_area :content, :class => "tinymce", :rows => 10, :cols => 40 %>
        <%= tinymce %>
      </div>
  </div>

</div>





<div class="panel panel-primary">
  <!-- Default panel contents -->
  <div class="panel-heading">Lecture Details </div>
  <div class="panel-body">
      <div class="field">
        <%= f.file_field :attachment %><br>
        <%= f.hidden_field :attachment_cache %>
      </div>

      <div class="field">
        <%= f.label :course_id %><br>

        <%= f.collection_select(:course_id, Course.all, :id, :title, prompt: true, class: "btn btn-default dropdown-toggle" ) %>

      </div>

  </div>

</div>








  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
